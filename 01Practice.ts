var a: number;

a = 312

type user = {
    name: string;
    fun:()=>number
}

let n: user = {
    name: "paba",
    fun: () => {
        return 4;
    }
}

// type casting / type assertion both are same

//unknown type is also their